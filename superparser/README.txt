Usage:

import superparser as sp
d = sp.parse_mayak()

-------------------------

Теперь d содержит всю инфу с Маяка:

d = { 'Имя Фамилия': {'text': [html-текст страницы про актера], 'imgurl': [список с ссылками на портреты актёра], 'vidurl': [список с ссылками на видео с актёром]} }

--------------------------

Что надо сделать:

Преобразовать 'text' для каждого актёра из сырой html=страницы в словарь со всей инфой